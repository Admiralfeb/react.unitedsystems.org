import { docsList } from '../data/docsList';
import { guidesList } from '../data/guidesList';
import { toolsList } from '../data/toolsList';
import { uscLinksList } from '../data/uscLinksList';

export const useInfoButtons = () => {
  return { docsList, guidesList, toolsList, uscLinksList };
};
